import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const PaymentScreens = () => {
  return (
    <View>
      <Text>PaymentScreens</Text>
    </View>
  );
};

export default PaymentScreens;

const styles = StyleSheet.create({});
